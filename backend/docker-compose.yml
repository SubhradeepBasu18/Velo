services:
  redis:
    image: redis:latest
    container_name: redis-for-queue
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --requirepass velo-redis-queue --appendonly yes
    restart: unless-stopped

volumes:
  redis_data:
    driver: local